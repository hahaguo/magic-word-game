# 🚀 阿里云容器镜像服务部署指南

## 版本信息

- **应用版本**: v2.1
- **Docker镜像**: magic-word-game:v2.1
- **适用平台**: 阿里云容器镜像服务（ACR）

---

## 📦 什么是阿里云容器镜像服务

阿里云容器镜像服务（Alibaba Cloud Container Registry，简称ACR）是一个安全的应用镜像托管平台，支持：

- ✅ 镜像的全球分发
- ✅ 安全的镜像扫描
- ✅ 稳定的国内网络访问
- ✅ 镜像加速服务
- ✅ 私有镜像仓库
- ✅ 与阿里云ECS、容器服务无缝集成

---

## 🎯 使用场景

### 适合使用阿里云ACR的情况：

1. **服务器在国内** - 访问速度快，稳定可靠
2. **团队协作** - 多人共享镜像
3. **生产环境部署** - 需要稳定的镜像仓库
4. **私有项目** - 需要私密的镜像存储
5. **自动构建** - 与CI/CD集成

### 不推荐使用的情况：

- 个人学习项目（可以用Docker Hub）
- 服务器在海外（建议用Docker Hub或AWS ECR）

---

## 🔧 前置准备

### 1. 注册阿里云账号

访问：https://www.aliyun.com

### 2. 开通容器镜像服务

1. 登录阿里云控制台
2. 搜索"容器镜像服务"
3. 点击"立即开通"（个人版免费）

访问：https://cr.console.aliyun.com

### 3. 创建命名空间

命名空间用于组织镜像，类似于文件夹。

```
1. 进入容器镜像服务控制台
2. 选择"默认实例" -> "命名空间"
3. 点击"创建命名空间"
4. 输入命名空间名称（如：word-game）
5. 设置为"公开"或"私有"
   - 公开：任何人都可以拉取镜像
   - 私有：需要登录才能拉取
```

**推荐配置：**
- 命名空间：`word-game` 或 `edu`
- 可见性：公开（方便分享）或私有（需要保密）

### 4. 创建镜像仓库

```
1. 进入"镜像仓库" -> "镜像仓库"
2. 点击"创建镜像仓库"
3. 选择命名空间
4. 仓库名称：magic-word-game
5. 摘要：墨墨的魔法单词王国 - 小学英语学习游戏
6. 仓库类型：公开/私有
7. 点击"下一步" -> "创建镜像仓库"
```

### 5. 设置访问凭证（固定密码）

```
1. 点击右上角头像
2. 选择"访问凭证"
3. 点击"设置固定密码"
4. 设置并记住密码（不是阿里云账号密码）
5. 记录用户名（格式：阿里云账号@命名空间）
```

**重要提示：**
- 容器镜像服务的登录密码是独立的固定密码
- 不是阿里云账号的登录密码
- 必须先设置固定密码才能推送镜像

---

## 🚀 推送镜像到阿里云

### 方法1：使用自动脚本（推荐）

#### 步骤1：配置脚本

编辑 `push-to-aliyun.sh` 文件，修改配置：

```bash
# 修改这些配置
ALIYUN_REGISTRY="registry.cn-beijing.aliyuncs.com"  # 镜像仓库地址
ALIYUN_NAMESPACE="word-game"                        # 你的命名空间
ALIYUN_REPO="magic-word-game"                       # 仓库名称
VERSION="v2.1"                                      # 版本号
```

**镜像仓库地址选择：**
- 北京：`registry.cn-beijing.aliyuncs.com`
- 杭州：`registry.cn-hangzhou.aliyuncs.com`
- 上海：`registry.cn-shanghai.aliyuncs.com`
- 深圳：`registry.cn-shenzhen.aliyuncs.com`

选择离你最近的区域获得最佳速度。

#### 步骤2：运行脚本

```bash
# 赋予执行权限
chmod +x push-to-aliyun.sh

# 运行脚本
./push-to-aliyun.sh

# 输入容器镜像服务的固定密码（不是阿里云账号密码）
```

脚本会自动：
1. 检查配置
2. 登录阿里云
3. 构建镜像（如果不存在）
4. 给镜像打标签
5. 推送到阿里云
6. 显示拉取命令

### 方法2：手动推送

#### 步骤1：登录阿里云

```bash
# 替换为你的信息
docker login --username=你的用户名 registry.cn-beijing.aliyuncs.com

# 输入容器镜像服务的固定密码
```

#### 步骤2：构建镜像（如果还没构建）

```bash
./deploy.sh
```

#### 步骤3：打标签

```bash
# 格式：registry地址/命名空间/仓库名:版本号
docker tag magic-word-game:v2.1 \
  registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1

# 同时打latest标签
docker tag magic-word-game:v2.1 \
  registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:latest
```

#### 步骤4：推送镜像

```bash
# 推送版本标签
docker push registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1

# 推送latest标签
docker push registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:latest
```

---

## 📥 从阿里云拉取镜像

### 公开镜像（无需登录）

```bash
# 拉取指定版本
docker pull registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1

# 拉取最新版本
docker pull registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:latest
```

### 私有镜像（需要登录）

```bash
# 先登录
docker login --username=你的用户名 registry.cn-beijing.aliyuncs.com

# 再拉取
docker pull registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1
```

---

## 🎮 运行从阿里云拉取的镜像

### 基本运行

```bash
docker run -d \
  -p 8080:80 \
  --name magic-word-game \
  --restart unless-stopped \
  -e TZ=Asia/Shanghai \
  registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1
```

### 修改docker-compose.yml使用阿里云镜像

```yaml
version: '3.8'

services:
  word-game:
    # 使用阿里云镜像
    image: registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1
    container_name: magic-word-game
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
```

然后运行：

```bash
docker-compose up -d
```

---

## 🔍 管理阿里云镜像

### 在控制台查看

1. 访问：https://cr.console.aliyun.com
2. 选择"镜像仓库" -> "镜像仓库"
3. 点击你的仓库名称
4. 可以看到：
   - 所有版本标签
   - 镜像大小
   - 推送时间
   - 拉取统计

### 删除镜像版本

```bash
# 在阿里云控制台删除
1. 进入镜像仓库详情
2. 选择"标签管理"
3. 选择要删除的版本
4. 点击"删除"
```

### 设置镜像为公开/私有

```bash
1. 进入镜像仓库详情
2. 点击"修改仓库"
3. 选择"公开"或"私有"
4. 保存
```

---

## 🌐 镜像加速器配置

阿里云提供免费的镜像加速服务，加快拉取速度。

### 获取加速器地址

1. 访问：https://cr.console.aliyun.com
2. 左侧菜单 -> "镜像工具" -> "镜像加速器"
3. 复制你的专属加速器地址

### 配置Docker使用加速器

#### macOS/Windows（Docker Desktop）

```
1. 打开Docker Desktop
2. 点击设置图标 -> "Docker Engine"
3. 添加：
{
  "registry-mirrors": [
    "https://你的加速器地址.mirror.aliyuncs.com"
  ]
}
4. 点击"Apply & Restart"
```

#### Linux

```bash
# 创建配置文件
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<-'EOF'
{
  "registry-mirrors": [
    "https://你的加速器地址.mirror.aliyuncs.com"
  ]
}
EOF

# 重启Docker
sudo systemctl daemon-reload
sudo systemctl restart docker
```

---

## 💡 常见问题

### Q1: 登录时提示"unauthorized"

**原因：** 使用了错误的密码

**解决：**
1. 确认使用的是容器镜像服务的"固定密码"
2. 不是阿里云账号的登录密码
3. 访问 https://cr.console.aliyun.com，点击头像 -> "访问凭证"
4. 重新设置固定密码

### Q2: 推送镜像时提示"denied"

**原因：** 仓库不存在或没有权限

**解决：**
1. 确认已创建命名空间和镜像仓库
2. 确认镜像标签格式正确：`registry地址/命名空间/仓库名:版本号`
3. 确认已登录

### Q3: 拉取镜像速度慢

**解决：**
1. 配置阿里云镜像加速器（见上文）
2. 选择离你最近的区域（cn-beijing、cn-hangzhou等）
3. 检查网络连接

### Q4: 镜像推送成功但无法拉取

**原因：** 镜像仓库设置为私有，但未登录

**解决：**
1. 如果是私有仓库，拉取前先登录
2. 或将仓库设置为公开

### Q5: 如何删除本地的阿里云镜像

```bash
# 查看本地镜像
docker images | grep aliyuncs

# 删除镜像
docker rmi registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1
```

---

## 📊 费用说明

### 个人版（免费）

- ✅ 命名空间：3个
- ✅ 仓库数量：300个
- ✅ 存储空间：10GB
- ✅ 流量：100GB/月
- ✅ 构建规则：无限制

**对于本项目完全够用！**

### 企业版（付费）

- 更大存储空间
- 更多并发构建
- 企业级SLA保障
- 镜像安全扫描

**个人学习项目不需要**

---

## 🎯 最佳实践

### 1. 版本管理

```bash
# 总是推送明确的版本号
docker push registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1

# 同时更新latest标签
docker push registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:latest
```

### 2. 镜像命名

```
格式：registry/namespace/repository:tag

示例：
registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1
│                                 │        │                 │
└─ 阿里云镜像仓库地址               └─命名空间  └─仓库名         └─版本标签
```

### 3. 安全建议

- ❌ 不要将固定密码写入脚本提交到git
- ✅ 使用环境变量存储密码
- ✅ 私密项目使用私有仓库
- ✅ 定期更换固定密码

### 4. 部署流程

```bash
# 开发阶段
./deploy.sh  # 本地构建测试

# 准备发布
./push-to-aliyun.sh  # 推送到阿里云

# 生产部署
docker pull registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1
docker-compose up -d
```

---

## 📝 完整部署示例

### 首次部署

```bash
# 1. 在阿里云创建命名空间和仓库（网页操作）

# 2. 配置推送脚本
vim push-to-aliyun.sh
# 修改 ALIYUN_REGISTRY、ALIYUN_NAMESPACE 等

# 3. 推送镜像
chmod +x push-to-aliyun.sh
./push-to-aliyun.sh

# 4. 在服务器上拉取运行
docker pull registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1
docker run -d -p 8080:80 --name magic-word-game \
  registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.1

# 5. 访问
http://服务器IP:8080
```

### 更新版本

```bash
# 1. 修改代码

# 2. 本地测试
./deploy.sh

# 3. 修改版本号
vim push-to-aliyun.sh  # 改为 v2.2

# 4. 推送新版本
./push-to-aliyun.sh

# 5. 服务器更新
docker pull registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.2
docker stop magic-word-game
docker rm magic-word-game
docker run -d -p 8080:80 --name magic-word-game \
  registry.cn-beijing.aliyuncs.com/word-game/magic-word-game:v2.2
```

---

## 🔗 相关链接

- **阿里云容器镜像服务控制台**: https://cr.console.aliyun.com
- **容器镜像服务文档**: https://help.aliyun.com/product/60716.html
- **镜像加速器**: https://cr.console.aliyun.com/cn-hangzhou/instances/mirrors
- **Docker官方文档**: https://docs.docker.com

---

## 📞 技术支持

如有问题：
1. 查看阿里云容器镜像服务文档
2. 检查推送脚本的错误输出
3. 确认固定密码是否设置正确
4. 确认命名空间和仓库是否创建

---

**版本**: v2.1
**镜像**: magic-word-game:v2.1
**阿里云**: 容器镜像服务（ACR）

祝墨墨学习愉快！部署顺利！🎉✨💖
